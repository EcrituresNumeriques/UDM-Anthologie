'use strict';

/**
 * Seed files allow you to populate your database with test or seed data
 * independent of your migration files.
 */

exports.seed = function(connection, Promise) {
  return Promise.all([
    
    /**
     * Insert, update or delete data for the `user` table.
     */

    // Data to insert.
    // connection('user').insert(),

    // Data to update.
    // connection('user').where().update(),

    // Data to delete.
    // connection('user').where().del(),
    
  ]);
};
